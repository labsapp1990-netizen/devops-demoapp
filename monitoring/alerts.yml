groups:
- name: demoapp
  rules:
  - alert: AppDown
    expr: up{job="app"} == 0
    for: 3m
    labels:
      severity: critical
    annotations:
      summary: "Приложение недоступно"
      description: "Приложение app не отвечает более 3 минут"
